<div class="panel panel-aside">
<!-- 
	<header>
		<%#= link_to "new link", new_workspace_link_path %>
		<input /> search
	</header>
-->

	<ul class="list">
		<% collection.links.order("created_at DESC").each do |link| %>
		<li class="link">
			<hgroup>
				<h4>
					<%= link_to collection_route(collection).link_path(link) do %>
						<%= link.title.present? ? link.title : link.url %>
					<% end %>
				</h4>
			</hgroup>
			<p class="labels">
				<% link.tags.each do |tag| %>
					<span class="label"><%= tag.name %></span>
				<% end %>
			</p>

			<div class="actions-wrapper">
			<div class="actions">
				<%= link_to link.url do %><i class="icon-external-link"></i><% end %>
			</div>
			</div>
		</li>
		<% end %>
	</ul>
	
</div>